    <div class="content">
      <div class="page-title">
		<i class="icon-custom-left"></i>
        <% if(typeof asset != 'undefined') { %><h3>Raport ordine lansate in sedinta activului <%- req.session.getTranslation(asset.Name) %></h3><% } %>
      </div>
      <div class="row-fluid">
        <div class="span12">
          <div class="grid simple">
			<div class="grid-title no-border">
				<%- toolsService.getFlashMessage(req.flash()) %>
				
				<div class="clearfix"></div>
			</div>
			<div class="grid-body no-border">
				<table class="table table-hover table-condensed admin-table" id="example-table">
					<thead>
						<tr>
							<th class="v-align-middle" style="width:10%">Ring</th>
							<th class="v-align-middle" style="width:10%">Activ</th>
							<th class="v-align-middle" style="width:5%">Directie</th>
							<th class="v-align-middle" style="width:10%">Cantitate</th>
							<th class="v-align-middle" style="width:10%">Pret</th>
							<th class="v-align-middle" style="width:10%">Data expirare</th>
							<th class="v-align-middle" style="width:10%">Status ordin</th>
						</tr>
					</thead>
					<tbody>
						<% if(items.length>0) { _.each(items,function(item) { %>
						<tr>
							<td class="v-align-middle"><%= req.session.getTranslation(req.session.rings[toolsService.searchIdInArray(item.ID_Ring,req.session.rings)]['Name']) %></td>
							<td class="v-align-middle"><%= (toolsService.searchIdInArray(item.ID_Asset,req.session.assets)>-1 ? req.session.getTranslation(req.session.assets[toolsService.searchIdInArray(item.ID_Asset,req.session.assets)]['Name']) : '-' ) %></td>
							<td class="v-align-middle"><%- (item.Direction=='B'?'BID':'ASK') %></td>
							<td class="v-align-middle"><%= item.Quantity %></td>
							<td class="v-align-middle"><%= item.Price %></td>
							<td class="v-align-middle"><%= item.ExpirationDate %></td>
							<td class="v-align-middle">
								<%= (item.isTransacted ==1)?'Tranzactionat':'Netranzactionat' %>
							</td>
						</tr>
						<% }); } %>
					</tbody>
				</table>
			</div>
          </div>
          <div class="form-actions">
					<div class="text-center">
						<a class="btn btn-white btn-cons" href="javascript:history.back()">Inapoi</a>
					</div>
				</div>
        </div>
      </div>
    </div>
